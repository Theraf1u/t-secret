# Security Policy

Report vulnerabilities privately to the operator of the affected installation. Do not include real secrets, bot tokens, SSH keys, database dumps, `MASTER_KEY`, or production `.env` files in reports.

Rotate exposed credentials immediately. A database backup and its corresponding `MASTER_KEY` must never be stored in the same security domain.
